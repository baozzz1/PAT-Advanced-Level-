#include<iostream>
#include<string>
#include<vector>
using namespace std;

//Graph
int P1003() {
	int N, M, C1, C2;
	cin >> N >> M >> C1 >> C2;
	vector<int> rescueTeams(N);
	for (int i = 0; i < N; i++)
		cin >> rescueTeams[i];
	int c1, c2, L;
	for (int i = 0; i < M; i++) {
		cin >> c1 >> c2 >> L;

	}
	return 0;
}